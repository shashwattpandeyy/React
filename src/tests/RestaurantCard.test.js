import { render } from "@testing-library/react"
import RestaurantCard from "../Components/RestaurantCard"

it("Should render restaurant card with props data", () => {
  // TODO: Pass with props data
  //  render(<RestaurantCard />)
})